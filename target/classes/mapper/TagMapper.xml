<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.daiqi.mapper.TagMapper">
  <select id="countCardsByTag" resultType="com.daiqi.dto.TagCountRow">
    select t.id as id, count(ct.card_id) as cnt
    from tag t left join card_tag ct on t.id = ct.tag_id
    group by t.id
  </select>

  <select id="countCardsByTagId" parameterType="long" resultType="long">
    select count(1) from card_tag where tag_id = #{tagId}
  </select>
</mapper>
